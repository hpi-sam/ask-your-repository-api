version: 2
jobs:
  test:
    docker:
      - image: circleci/python:3.7
        environment:
          NEO4J_URL: bolt://localhost:7687
      - image: neo4j:3.4.5
        environment:
          NEO4J_AUTH: none
          NEO4J_dbms_memory_pagecache_size: 100M
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "Pipfile" }}
      - run: pipenv install --dev
      - save_cache:
          key: dependency-cache-{{ checksum "Pipfile" }}
          paths:
            - ~/.cache/pip
      - run: pipenv run coverage run --source=application -m mamba.cli specs
      - run: pipenv run coveralls
  lint:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run: sudo pip install pipenv && pipenv install --dev
      - run: pipenv run pylint application/ specs/
  e2e:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: docker-compose -f .circleci/docker-compose.ci.yml up -d
      - run: docker-compose -f .circleci/docker-compose.ci.yml exec jona yarn cypress run
  build:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - setup_remote_docker
      - run: echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
      - run: docker build -t bp2018hg1/elija:$CIRCLE_WORKFLOW_ID -t bp2018hg1/elija:latest .
      - run: docker push bp2018hg1/elija:$CIRCLE_WORKFLOW_ID
      - run: docker push bp2018hg1/elija:latest
  deploy:
    docker:
      - image: dtzar/helm-kubectl:2.11.0
    steps:
      - run: kubectl set image deployment elija elija=bp2018hg1/elija:$CIRCLE_WORKFLOW_ID --server=$KUBERNETES_SERVER --token=$KUBERNETES_BEARER_TOKEN
workflows:
  version: 2
  pipeline:
    jobs:
      - test
      - lint
      - e2e
      - build:
          requires:
            - test
            - lint
            - e2e
          filters:
            branches:
              only: master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
